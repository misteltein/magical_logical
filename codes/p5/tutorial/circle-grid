const sketch = (p5: P5) => {
    p5.setup = () => {
        p5.createCanvas(300, 100);
        p5.background(51);
        const d = 50;
        for (let x = 0; x <= 300; x += 50) {
            for (let y = 0; y <= 100; y += 50) {
                p5.circle(x, y, d);
            }
        }
    };
};
